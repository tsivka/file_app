<% flash.each do |key, msg| %>
    <%= content_tag :div, msg, :class => [key, " message"], :id => "notice_#{key}" %>
<% end %>
<%= form_tag '/uploader/create', { :multipart => true } do %>
    <p>
      <%= file_field_tag 'uploader[file]' %>
    </p>
    <p>
      <%= submit_tag "Upload" %>
    </p>
<% end %>
